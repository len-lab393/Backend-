rm -rf node_modules package-lock.json   # delete broken installs
npm install                             # install all packages from package.json
npm install mongoose                     # explicitly install mongoose
